let idade:Number = 27 ; //criei uma constante que armazena NUMEROS e esta armazenando 27 e o nome da constante é idade
const nome:string = 'Lucas';
const maiorDeIdade = true;

function soma (num1 : number, num2 : number):number {
  const soma: number = num1 + num2; 
  return soma
}

document.write(`A SOMA DE 7+5 É DE ${soma(7,5)}`);